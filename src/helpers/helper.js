

function logIn(emailId, password) {
    // check user already exists
    // if not
    // ... show popup that user doesnt exist
    // else
    return true;
}


function logOut() {
    return true;
}

function register(name, emailId, phoneNo, password) {
    // add user to db
    // on failure ... shor popup
    // else
    return true;
}

export {logIn, logOut, register};